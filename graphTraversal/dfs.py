# Uses: peer to peer networking, web crawlers, finding closest matches or recommendations, shortest path
def dfs(vertex):
    result = []
    visited = {}
    
    def dfs_helper(vertex):
        return
    return result